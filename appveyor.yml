build: off

environment:
  nodejs_version: "8.9.0"

install:
  - git submodule update --init --recursive
  - yarn install
  - yarn install-node-obs
  - yarn ci:compile

test_script:
  - yarn test -- -v
