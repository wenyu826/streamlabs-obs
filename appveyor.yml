build: off

environment:
  nodejs_version: "8.7.0"

install:
  - node -v
  - git submodule update --init --recursive
  - yarn install
  - yarn install-node-obs
  - yarn ci:compile

test_script:
  - yarn test -- -v
